#!/bin/bash

# Restart the Artillery server
sudo /var/artillery/restart_server.py

# Run the Artillery main program and send it to the background
#sudo nohup /var/artillery/artillery.py &
sudo /var/artillery/artillery.py

#echo "Artillery has been started in the background."
